Knockout.js examples in TypeScript
==================================

This repository contains Knockout.js examples converted to TypeScript with strong-typing intact.

Assumes Node.js is installed.  Required global npm items:
  * npm install -g bower
  * npm install -g tsd

